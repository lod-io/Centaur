package constants

// API related constants
const (
	BaseURL                 = "https://api.clod.io/v1"
	ChatCompletionsEndpoint = BaseURL + "/chat/completions"
	ModelsEndpoint          = BaseURL + "/providers/models"
)

// Add other constant groups as needed
